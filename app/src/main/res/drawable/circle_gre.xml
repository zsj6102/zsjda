<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 填充颜色 -->
    <solid android:color="@color/main_gre"></solid>

    <!-- 线的宽度，颜色灰色 -->
    <stroke android:width="0.2dp" android:color="@color/main_gre"></stroke>

    <size android:width="18dp"
          android:height="18dp"/>

</shape>